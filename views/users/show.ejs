<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= profileUser.username %>'s Profile</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= profileUser.username %>'s Recipes</h1>
    
    <% if (recipes.length === 0) { %>
        <p>No recipes shared yet.</p>
    <% } else { %>
        <ul>
            <% recipes.forEach(recipe => { %>
                <li>
                    <h3><a href="/recipes/<%= recipe._id %>"><%= recipe.title %></a></h3>
                    <p><%= recipe.description %></p>
                    <p>Cooking Time: <%= recipe.cookingTime %> minutes | Servings: <%= recipe.servings %></p>
                    <% if (recipe.ingredients && recipe.ingredients.length > 0) { %>
                        <p>Ingredients: <%= recipe.ingredients.map(i => i.name).join(', ') %></p>
                    <% } %>
                </li>
            <% }) %>
        </ul>
    <% } %>
    
    <a href="/users">Back to Community</a>
</body>
</html>